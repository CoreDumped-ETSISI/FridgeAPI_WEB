extends ../header_layout

block main-content
    .col.s12.m8.offset-m2.l4.offset-l4.z-depth-4.card-panel(style='padding: 2em')
        form.login-form
            .row(style='margin-bottom: 3em')
                .input-field
                    select#comboProducts
                        option(value="" disabled selected) ...
                    label Product list
            .row(style='margin-bottom: 0.5em')
                img#productImage.circle.responsive-img.col.s10.offset-s1.m6.offset-m3.l8.offset-l2(src="/images/default-product-image.jpg" style="object-fit: cover; padding: 0;")
                input#fileElem(type="file" accept="image/*" style="display:none" onchange="handleFiles(this.files,'productImage')")
            .row(style='margin-bottom: 0.5em')
                .input-field
                    input#productName(type='text')
                    label(for="productName") Name
            .row(style='margin-bottom: 0.5em')
                .input-field
                    input#productStock(type='text')
                    label(for="productStock") Stock
            .row(style='margin-bottom: 0.5em')
                .input-field
                    input#productPrice(type='number' min='0' step='0.01')
                    label(for="productPrice") Price
            .row(style='margin-bottom: 0.5em')
                .input-field
                    input#productUnits(type='number' min='0')
                    label(for="productUnits") Units (only used to calculate the price)
            .row.margin-bottom
                .col.s12.l8.push-l4.margin-bottom
                    a.btn.verde-core.waves-effect.waves-light.texto-oscuro.col.s12(onclick='sendStock()') UPDATE
                .col.s4.l4.pull-l8
                    a.btn.rojo.waves-effect.waves-light.texto-oscuro.col.s12(href='/admin')
                        i.material-icons.left.tiny arrow_back
                        span back
            .row.margin-bottom.center
                include ../switch.pug

block scripts
    script(src='/js/croppie/croppie.js')
    script(src='/js/imageUploader.js')
    script(src='/js/admin/admin_update_product.js')
